"use client"

import { useState } from "react"
import { Button } from "@/components/ui/button"
import { WalletConnectionModal } from "@/components/wallet-connection-modal"
import { ArrowRight, Award, CheckCircle, Shield } from "lucide-react"

export default function Home() {
  const [isWalletModalOpen, setIsWalletModalOpen] = useState(false)
  const [connectedWallet, setConnectedWallet] = useState<string | null>(null)

  const handleWalletConnect = (walletType: string) => {
    setConnectedWallet(walletType)
  }

  return (
    <div className="flex min-h-screen flex-col">
      {/* Header */}
      <header className="sticky top-0 z-40 border-b bg-background">
        <div className="container flex h-16 items-center justify-between">
          <div className="flex items-center gap-2">
            <Award className="h-6 w-6 text-blue-600" />
            <span className="text-xl font-bold">Achievo</span>
          </div>
          <div className="flex items-center gap-4">
            <nav className="hidden md:flex">
              <ul className="flex items-center gap-6">
                <li>
                  <a href="#" className="text-sm font-medium hover:text-blue-600">
                    Home
                  </a>
                </li>
                <li>
                  <a href="#" className="text-sm font-medium hover:text-blue-600">
                    Marketplace
                  </a>
                </li>
                <li>
                  <a href="#" className="text-sm font-medium hover:text-blue-600">
                    About
                  </a>
                </li>
                <li>
                  <a href="#" className="text-sm font-medium hover:text-blue-600">
                    FAQ
                  </a>
                </li>
              </ul>
            </nav>
            {connectedWallet ? (
              <Button variant="outline" className="gap-2">
                <div className="h-2 w-2 rounded-full bg-green-500"></div>
                {connectedWallet === "meteor" ? "Meteor" : "MyNear"} Connected
              </Button>
            ) : (
              <Button onClick={() => setIsWalletModalOpen(true)}>Connect Wallet</Button>
            )}
          </div>
        </div>
      </header>

      {/* Hero Section */}
      <section className="bg-gradient-to-b from-blue-50 to-white py-20">
        <div className="container flex flex-col items-center gap-8 text-center">
          <h1 className="text-4xl font-bold tracking-tight sm:text-5xl md:text-6xl">
            Secure Digital Credentials on <span className="text-blue-600">Blockchain</span>
          </h1>
          <p className="max-w-2xl text-lg text-muted-foreground">
            Achievo leverages NEAR Protocol to provide tamper-proof certificates and rewards for learners and
            organizations.
          </p>
          <div className="flex flex-col gap-4 sm:flex-row">
            <Button size="lg" onClick={() => setIsWalletModalOpen(true)}>
              Connect Wallet
            </Button>
            <Button size="lg" variant="outline">
              Learn More
            </Button>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section className="py-20">
        <div className="container">
          <h2 className="mb-12 text-center text-3xl font-bold">Platform Benefits</h2>
          <div className="grid gap-8 md:grid-cols-3">
            <div className="flex flex-col items-center gap-4 rounded-lg border p-6 text-center">
              <div className="rounded-full bg-blue-100 p-3">
                <Shield className="h-6 w-6 text-blue-600" />
              </div>
              <h3 className="text-xl font-bold">Secure Authentication</h3>
              <p className="text-muted-foreground">
                Blockchain-based verification ensures your credentials cannot be forged or tampered with.
              </p>
            </div>
            <div className="flex flex-col items-center gap-4 rounded-lg border p-6 text-center">
              <div className="rounded-full bg-green-100 p-3">
                <Award className="h-6 w-6 text-green-600" />
              </div>
              <h3 className="text-xl font-bold">Dynamic NFT Certificates</h3>
              <p className="text-muted-foreground">
                Certificates evolve as you complete more courses, showcasing your growing expertise.
              </p>
            </div>
            <div className="flex flex-col items-center gap-4 rounded-lg border p-6 text-center">
              <div className="rounded-full bg-purple-100 p-3">
                <CheckCircle className="h-6 w-6 text-purple-600" />
              </div>
              <h3 className="text-xl font-bold">Instant Verification</h3>
              <p className="text-muted-foreground">
                Employers can instantly verify your credentials with a simple certificate ID.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="bg-blue-600 py-16 text-white">
        <div className="container flex flex-col items-center gap-6 text-center">
          <h2 className="text-3xl font-bold">Ready to Get Started?</h2>
          <p className="max-w-2xl text-blue-100">
            Join thousands of learners and organizations already using Achievo for secure digital credentials.
          </p>
          <Button size="lg" variant="secondary" onClick={() => setIsWalletModalOpen(true)} className="mt-4">
            Connect Wallet <ArrowRight className="ml-2 h-4 w-4" />
          </Button>
        </div>
      </section>

      {/* Footer */}
      <footer className="border-t py-12">
        <div className="container">
          <div className="flex flex-col items-center justify-between gap-6 md:flex-row">
            <div className="flex items-center gap-2">
              <Award className="h-6 w-6 text-blue-600" />
              <span className="text-xl font-bold">Achievo</span>
            </div>
            <div className="flex gap-8">
              <a href="#" className="text-sm text-muted-foreground hover:text-blue-600">
                Terms
              </a>
              <a href="#" className="text-sm text-muted-foreground hover:text-blue-600">
                Privacy
              </a>
              <a href="#" className="text-sm text-muted-foreground hover:text-blue-600">
                Contact
              </a>
            </div>
            <div className="text-sm text-muted-foreground">© 2023 Achievo. All rights reserved.</div>
          </div>
        </div>
      </footer>

      {/* Wallet Connection Modal */}
      <WalletConnectionModal
        open={isWalletModalOpen}
        onOpenChange={setIsWalletModalOpen}
        onConnect={handleWalletConnect}
      />
    </div>
  )
}
